# build output (committed deliberately at release packaging, M10)
/bin/
*.o
*.obj
*.exe.stackdump

# nix
/result
/.direnv/

# scratch
/tmp/
/tests/out/

# time-machine ring exports (the scrubber's "save .ptrace"; share via
# tests/traces or the feed, don't commit by accident)
/projects/*/ring-f*.ptrace
